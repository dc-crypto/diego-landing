import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Diego Castro — Tecnología para hacer crecer tu negocio",
};

export default function ProyectosLayout({ children }: { children: React.ReactNode }) {
  return children;
}
